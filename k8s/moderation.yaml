apiVersion: apps/v1
kind: Deployment
metadata:
  name: moderation-service
  namespace: default
  labels:
    app: moderation-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: moderation-service
  template:
    metadata:
      labels:
        app: moderation-service
    spec:
      containers:
        - name: moderation-service
          image: moderation-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
              name: http
          env:
            - name: PORT
              value: "3004"
            - name: DATABASE_URL
              value: postgresql://postgres:postgres@postgres:5432/app
            - name: NATS_URL
              value: nats://nats:4222
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: default
  labels:
    app: moderation-service
spec:
  selector:
    app: moderation-service
  ports:
    - port: 3004
      targetPort: 3004
      name: http
